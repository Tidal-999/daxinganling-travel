# 📱 大兴安岭赏秋攻略 App 使用说明

一个可以直接装在手机上的可编辑旅行攻略（网页应用，无需应用商店）。

## 永久链接（已部署到 GitHub Pages）

- 网址：**https://tidal-999.github.io/daxinganling-travel/**
- 二维码见 `qr-site.png`
- 任何网络、任何时间都能打开；HTTPS 环境自动缓存，首次打开后可离线查看
- 内容编辑保存在手机本地

## 怎么改内容

- 右上角 **✏️ 编辑** 进入编辑模式，所有文字直接改（时间、标题、说明、交通、待办、花费、贴士）
- **↑ ↓** 调整顺序、**🗑** 删除、**＋** 添加安排/新的一天，修改自动保存
- **⚙ 数据**：复制分享文本、导出/导入备份、恢复默认

## 本地运行（可选）

1. 双击 `启动.bat`（或 `node server.js`），浏览器打开 http://localhost:8080
2. 手机与电脑连同一 WiFi，手机浏览器打开电脑显示地址，添加到主屏幕

## 文件说明

| 文件 | 作用 |
|---|---|
| index.html | 攻略 App 本体（单文件） |
| manifest.json / sw.js / icon-*.png | 手机安装成 App 所需 |
| server.js / 启动.bat | 本地运行 |
| qr-site.png | 永久链接二维码 |

## 想改攻略内容并同步到线上？

在 Codex 里说"更新线上的攻略"即可，会帮你改好并重新发布。
## 路线图

- 行程页顶部有 **7天总路线图**（彩色分段对应 DAY1-7，含起点/终点、界河示意）
- 每天卡片内自带 **当日路线图**（当天路段高亮显示，其余路段灰色作参照）
- 图为示意图（非精确比例），仅供行程方向参考；如需高德/百度导航，按各天行程标题搜索即可
## 多人协作：别人改完怎么更新网址

原理：网址展示的内容来自仓库里的 `data.json`（App 打开时会自动读取；有本地修改则本地优先，可用"从线上拉取"手动同步）。

**别人改行程并让网址更新（3 步）：**
1. 打开网址 → ✏️ 编辑 → 改好内容
2. ⚙ 数据 → 点 **"📤 导出 data.json"**（下载一个 data.json 文件）
3. 把文件上传到 GitHub 仓库根目录（github.com/Tidal-999/daxinganling-travel → Add file → Upload files → 选文件 → Commit），等约 1 分钟，所有人再打开网址就是新版
   - 不会操作 GitHub？直接把 data.json 文件发到 Codex，说"更新线上的攻略"即可代传

**想立即看到线上最新版？** ⚙ 数据 → 点 **"🌐 从线上拉取最新攻略"**

> 注意：data.json 一旦更新，新访客和点了"从线上拉取"的人会看到新版；不想被覆盖的本地修改请先导出备份。
## 自动云同步（所有人实时共享）

- App 已接入共享云端：**任何人在网页里编辑，约3秒后自动保存到云端**；其他人打开/刷新网页，或等待约60秒自动刷新，即可看到最新版（无需令牌、无需上传文件）
- 云端内容同时是"网址默认版"的实时来源；GitHub 的 data.json 作为初始内容兜底
- 编辑内容优先本地保存；云端不可用时自动降级为仅本地编辑